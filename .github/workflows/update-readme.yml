name: 🧠 Actualizar README con commits recientes

on:
  schedule:
    - cron: "0 */6 * * *" # Cada 6 horas
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 🧩 Clonar repositorio
        uses: actions/checkout@v4

      - name: ⚙️ Configurar Git
        run: |
          git config user.name "readme-bot"
          git config user.email "bot@github.com"

      - name: 🪄 Generar lista de commits
        run: |
          echo "<!--COMMITS_SECTION_START-->" > commits.md
          echo "### Últimos commits 🧾" >> commits.md
          echo "" >> commits.md
          git log -5 --pretty=format:"- %s (%cr)" >> commits.md
          echo "" >> commits.md
          echo "<!--COMMITS_SECTION_END-->" >> commits.md

      - name: 🔄 Insertar commits en README
        run: |
          sed -i '/<!--COMMITS_SECTION_START-->/, /<!--COMMITS_SECTION_END-->/c\'"$(cat commits.md)" README.md

      - name: 🚀 Subir cambios
        env:
          GITHUB_TOKEN: ${{ secrets.README_TOKEN }}
        run: |
          git add README.md
          git commit -m "📝 Actualizando commits recientes" || echo "Sin cambios que subir"
          git push https://x-access-token:${{ secrets.README_TOKEN }}@github.com/${{ github.repository }}.git main
